/*const { taskDB } = require("./models/taskDataBase");

const taskAdder = (req, res) => {
  const { text } = req.body;
  taskDB.push({ id: taskDB.length + 1, text: text });
};

module.exports = taskAdder
*/